<nav class="navbar navbar-expand-lg">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('app_home_index') }}">LiveQuestion</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNavDropdown">
			{% if app.user %}
				<ul class="navbar-nav">
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_home_index') }}">Accueil</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_question_index') }}">Les questions</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_user_index') }}">Les auteurs</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_question_new') }}">Nouvelle question</a>
					</li>
				</ul>
				<ul class="navbar-nav ms-auto">
					<li class="nav-item dropdown">
						<a
							class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">{{ app.user.username }}
						</a>
						<ul class="dropdown-menu dropdown-menu-end">
							<li>
								<a class="dropdown-item" href="{{ path('app_my_account') }}">Mon compte</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_my_questions') }}">Mes questions</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
							</li>
						</ul>
					</li>
				</ul>
			{% endif %}

		</div>
	</div>
</nav>

<style>
	.navbar {
		padding: 1.5%;
		background-color: #AD0569;
	}

	.navbar-brand {
		font-size: 1.5rem;
		color: #FFFFFF;
	}

	.navbar-brand:hover {
		color: #FFD700;
	}

	.nav-item {
		font-size: 1.25rem;
	}

	.nav-link {
		color: #FFFFFF;
		transition: color 0.3s;
	}

	.nav-link:hover {
		color: #FFD700;
	}

	.dropdown-menu {
		background-color: #AD0569;
	}

	.dropdown-item {
		color: #FFFFFF;
	}

	.dropdown-item:hover {
		color: #AD0569;
		background-color: #FFD700;
	}

	.navbar-toggler {
		border: none;
	}

	.navbar-toggler-icon {
		background-color: #FFFFFF;
	}
</style>
